name: Greet

on:
  push:
    branches:
      - main

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Greet the user
        run: echo "Hello, ${{ github.actor }}! Welcome to GitHub Actions."
